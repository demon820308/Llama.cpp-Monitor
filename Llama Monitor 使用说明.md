# Llama Monitor 使用说明

## 1. 程序作用
Llama Monitor 是一个用于启动 `llama-server.exe` 的图形界面工具。
你不需要手动敲命令，只要在窗口里填写参数，点击 `Start Server` 就可以启动服务。

程序默认会读取和保存配置文件：
- `Llama Monitor-config.json`

目录版运行时，请确保以下文件在同一套目录结构中：
- `Llama Monitor.exe`
- `_internal` 文件夹

---

## 2. 启动前准备
在打开程序前，建议先准备好以下内容：

1. `llama-server.exe`
2. 模型文件 `.gguf`
3. 如果是视觉模型，还要准备 `mmproj` 文件
4. 建议在程序目录下建立 `models` 文件夹，并把模型放进去

推荐目录结构示例：

```text
Llama Monitor/
├─ Llama Monitor.exe
├─ _internal/
├─ llama-server.exe
├─ models/
│  ├─ your-model.gguf
│  └─ mmproj-file.gguf
```

---

## 3. 打开程序后怎么填写
建议按下面顺序填写。

### 第一步：填写服务器程序路径
`Server Path`
- 填写 `llama-server.exe` 的文件名或完整路径
- 如果 `llama-server.exe` 就在当前工作目录里，通常填：`llama-server.exe`

### 第二步：选择工作目录
`Working Dir`
- 这里是程序启动命令时的工作目录
- 一般建议保持为当前目录，也就是程序所在目录
- 如果你的 `llama-server.exe` 和 `models` 都在同一个目录体系里，通常不用改

### 第三步：选择模型文件
`Model File`
- 这里选择主模型 `.gguf`
- 如果下拉框显示“请把model放在models的目录下”，说明程序没有在 `models` 目录中找到模型文件

### 第四步：是否使用 MMProj
`MMProj File`
- 这是多模态投影文件，主要用于视觉模型
- 右边勾选框勾上时，启动命令里才会加上 `--mmproj`
- 如果你使用的是纯文本模型，可以取消勾选

### 第五步：设置上下文和 GPU
`Context Size`
- 设定上下文长度

`GPU Layers`
- 设定卸载到 GPU 的层数

### 第六步：设置监听地址
`Host`
- 决定服务监听的地址

`Port`
- 决定服务使用的端口

### 第七步：设置可选性能参数
以下参数右侧有勾选框，只有勾上才会生效：
- `Threads`
- `N Predict`
- `Temp`
- `Top P`
- `Batch`

### 第八步：启动服务
点击 `Start Server`
- 启动后可以在下方看到状态、日志、CPU、内存和 GPU 使用情况
- 如果要停止服务，点击 `Stop Server`
- 如果要用浏览器打开服务，点击 `Open Web`
- 如果要保存当前设置，点击 `Save`

---

## 4. 每个选项说明与建议数值

### Server Path
含义：
- `llama-server.exe` 的路径

当前默认值：
- `llama-server.exe`

建议：
- 如果程序和 `llama-server.exe` 在同一目录，直接保持默认即可
- 如果不在同一目录，请填写完整路径

---

### Working Dir
含义：
- 启动服务时使用的工作目录

当前默认值：
- 程序当前目录

建议：
- 推荐保持为程序目录
- 如果模型、日志、可执行文件都在这个目录体系内，最稳

---

### Model File
含义：
- 主模型文件 `.gguf`

当前行为：
- 从 `models` 目录自动扫描

建议：
- 把主模型放到 `models` 文件夹中
- 例如：`models\gemma-4-E4B-it-Q4_K_M.gguf`

---

### MMProj File
含义：
- 多模态投影文件
- 常用于视觉模型，如图文模型

当前行为：
- 从 `models` 目录自动扫描
- 右侧勾选框决定是否真正生效

建议：
- 纯文本模型：取消勾选
- 视觉模型：勾选，并选择对应 `mmproj` 文件

---

### Context Size
命令行参数：
- `-c`

含义：
- 模型一次能处理的上下文长度
- 值越大，能处理的历史内容越多，但会更占内存和显存

当前默认值：
- `8192`

建议数值：
- `4096`：显存或内存较紧张时
- `8192`：通用推荐值
- `16384`：需要更长上下文时
- 更高：仅在你确认硬件足够时再尝试

说明：
- 如果上下文设太大，可能导致速度变慢，甚至启动失败

---

### GPU Layers
命令行参数：
- `-ngl`

含义：
- 指定多少层卸载到 GPU
- 数值越高，通常越快，但更占显存

当前默认值：
- `60`

建议数值：
- `0`：完全用 CPU
- `20 ~ 40`：中低显存显卡可尝试
- `60`：当前默认通用值
- 更高：按你的显存情况尝试

说明：
- 如果显存不足，启动可能失败或自动回退
- 最稳的做法是从较低值开始慢慢加

---

### Host
命令行参数：
- `--host`

含义：
- 服务监听地址

当前默认值：
- `0.0.0.0`

建议数值：
- `127.0.0.1`：只允许本机访问，最安全
- `0.0.0.0`：允许局域网设备访问

说明：
- 如果你只在本机使用，推荐 `127.0.0.1`
- 如果你要让手机、平板或其他电脑访问，使用 `0.0.0.0`

---

### Port
命令行参数：
- `--port`

含义：
- 服务端口号

当前默认值：
- `8080`

建议数值：
- `8080`：默认推荐
- `8000`、`8081`、`9000`：如果端口冲突可以换

说明：
- 如果端口被占用，程序可能无法启动服务
- 启动后可通过浏览器访问类似地址：
  - `http://127.0.0.1:8080`

---

### Threads
命令行参数：
- `-t`

含义：
- 使用的 CPU 线程数
- 右侧勾选框勾上才生效

当前默认值：
- `8`

建议数值：
- 4 核 CPU：可试 `4`
- 8 线程 CPU：可试 `6 ~ 8`
- 更高核心数 CPU：可按物理核心或稍低于总线程数尝试

说明：
- 不是越大越好
- 开太高可能让系统变卡，或者收益不明显

---

### N Predict
命令行参数：
- `-n`

含义：
- 最多生成多少 token
- 右侧勾选框勾上才生效

当前默认值：
- `-1`

建议数值：
- `-1`：不限长度
- `256`：短回答
- `512`：一般回答
- `1024`：较长回答

说明：
- 如果你不希望模型一直生成，建议设成 `512` 或 `1024`

---

### Temp
命令行参数：
- `--temp`

含义：
- 温度，决定回答发散程度
- 右侧勾选框勾上才生效

当前默认值：
- `0.8`

建议数值：
- `0.2 ~ 0.5`：更稳、更保守
- `0.7 ~ 0.9`：通用
- `1.0` 以上：更发散、更随机

说明：
- 做问答、代码、推理时，一般建议适当低一点
- 做创意写作时，可以稍高一点

---

### Top P
命令行参数：
- `--top-p`

含义：
- 控制候选词采样范围
- 右侧勾选框勾上才生效

当前默认值：
- `0.95`

建议数值：
- `0.8 ~ 0.9`：更稳
- `0.95`：通用推荐
- `1.0`：几乎不过滤

说明：
- 通常和 `Temp` 一起影响输出风格
- 如果回答太飘，可以适当降低 `Top P`

---

### Batch
命令行参数：
- `-b`

含义：
- 批处理大小
- 右侧勾选框勾上才生效

当前默认值：
- `2048`

建议数值：
- `512`：保守
- `1024`：中等
- `2048`：当前默认通用值
- 更高：硬件较强时尝试

说明：
- 更高的 batch 有时可以提升吞吐
- 但也会增加显存或内存压力
- 如果遇到不稳定或资源不足，可先降到 `1024` 或 `512`

---

## 5. 常见使用场景推荐

### 场景 1：普通文本模型，稳定优先
- `MMProj`：取消勾选
- `Context Size`：`8192`
- `GPU Layers`：`20 ~ 60`，按显存调整
- `Host`：`127.0.0.1`
- `Port`：`8080`
- `Threads`：`8`
- `N Predict`：`512`
- `Temp`：`0.7`
- `Top P`：`0.95`
- `Batch`：`1024`

### 场景 2：视觉模型
- `MMProj`：勾选
- `Model File`：选择视觉主模型
- `MMProj File`：选择匹配的 `mmproj`
- 其他参数先保持默认，再根据硬件慢慢调

### 场景 3：低显存或低内存机器
- `Context Size`：`4096`
- `GPU Layers`：从 `0`、`20` 或 `30` 开始试
- `Batch`：`512` 或 `1024`
- `N Predict`：`256` 或 `512`

---

## 6. 常见问题

### 看不到模型文件
原因：
- `models` 目录里没有 `.gguf` 文件

处理方法：
1. 在程序目录下建立 `models` 文件夹
2. 把模型 `.gguf` 放进去
3. 重新打开程序

### MMProj 要不要勾选
结论：
- 纯文本模型不要勾选
- 视觉模型才勾选

### 为什么服务启动失败
常见原因：
- `llama-server.exe` 路径不对
- 模型文件不存在
- `mmproj` 与模型不匹配
- `GPU Layers` 太高导致显存不足
- `Context Size` 太大导致资源不足
- 端口被占用

### 为什么浏览器打不开
请检查：
- 服务是否已经成功启动
- `Host` 和 `Port` 是否填写正确
- 本机访问时可优先尝试：
  - `http://127.0.0.1:8080`

---

## 7. 当前界面参数与命令行对应关系
程序当前会生成的主要参数如下：

```bash
llama-server.exe -m <model> -c <ctx> -ngl <gpu_layers> --host <host> --port <port>
```

如果勾选了附加项，还会追加：

```bash
--mmproj <mmproj>
-t <threads>
-n <n_predict>
-b <batch>
--temp <temp>
--top-p <top_p>
```

---

## 8. 使用建议总结
如果你想先稳定跑起来，可以直接按这套填：

- `Server Path`：`llama-server.exe`
- `Working Dir`：保持默认
- `Model File`：选你的主模型
- `MMProj`：纯文本模型不勾，视觉模型勾上
- `Context Size`：`8192`
- `GPU Layers`：`60`
- `Host`：`127.0.0.1` 或 `0.0.0.0`
- `Port`：`8080`
- `Threads`：`8`
- `N Predict`：`512` 或 `-1`
- `Temp`：`0.8`
- `Top P`：`0.95`
- `Batch`：`1024` 或 `2048`

如果你刚开始不确定，建议先少改，先确认能成功启动，再一点点调参数。
